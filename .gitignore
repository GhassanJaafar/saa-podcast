# Root-level ignores. Each package (frontend/, studio/) keeps its own
# .gitignore for build output and dependencies.

# macOS
.DS_Store

# Editors
.idea/
*.swp

# Local env
.env
.env.production
.env.local
